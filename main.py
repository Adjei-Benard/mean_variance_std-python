from mean_var_std import calculate  # Import your function from the other file

# Create a list with 9 numbers
input_list = [0, 1, 2, 3, 4, 5, 6, 7, 8]

# Call the function with the list
result = calculate(input_list)

# Print the result in a readable way
from pprint import pprint
pprint(result)
